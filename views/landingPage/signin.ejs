<%- include('../includes/header0') %>

    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css"
            integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ=="
            crossorigin="anonymous" referrerpolicy="no-referrer" />
        <!-- <link rel="stylesheet" href="/css/landinPage/signin.css"> -->
        <title>Sign in</title>
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
            
            .back {
                position: absolute;
                top: 15vh;
                margin: 5px;
                padding: 3px;
                border: 3px solid var(--color1);
                border-radius: 10px;
                z-index: 1;
                opacity: 0.8;
            }
            .back a {
                text-decoration: none;
                color: var(--color1);
            }

            .container {
                position: absolute;
                top: 15vh;
                height: 85vh;
                width: 100vw;
                padding: 3rem;
                background-color: var(--color4);
                color: var(--color1);
                text-align: center;
            }

            .options {
                position: relative;
                width: 100%;
                height: 70%;
                padding: 2rem;
                display: flex;
                justify-content: space-around;
                align-items: center;
            }

            /* From Uiverse.io by ElSombrero2 */
            .card {
                width: max(200px, 40%);
                height: 100%;
                background: var(--color4);
                display: flex;
                justify-content: center;
                align-items: center;
                overflow: hidden;
                position: relative;
                box-shadow: 0px 0px 3px 1px var(--color3);
                border-radius: 5px;
                cursor: pointer;
                text-decoration: none;
            }

            .card .content {
                border-radius: 5px;
                background: var(--color4);
                width: calc(100% - 4px);
                height: calc(100% - 4px);
                z-index: 1;
                padding: 20px;
                color: var(--color1);
            }
            .card .content:hover {
                background-image: radial-gradient(#fff, transparent 95%);            
            }
            .card .content i {
                margin: 2rem;
                font-size: 5rem;
                color: var(--color2);
            }

            @property --angle {
                syntax: "<angle>";
                initial-value: 0deg;
                inherits: false;
            }

            .card::after,
            .card::before {
                opacity: 0;
                transition: opacity 300ms;
                content: '';
                height: 100%;
                width: 100%;
                position: absolute;
                
                background-image: conic-gradient(from var(--angle), transparent, var(--color3), transparent, var(--color3), transparent);
                padding: 3px;

                animation: spin 15s linear infinite;
                animation-play-state: paused;
            }
            .card:hover::before {
                opacity: 1;
                animation-play-state: running;
            }

            @keyframes spin {
                from {
                    --angle: 0deg;
                }

                to {
                    --angle: 360deg;
                }
            }

        </style>
    </head>

    <body>
        <main>
            <div class="back">
                <a href="#"><i class="fa-solid fa-arrow-left"></i> back</a>
            </div>
            <div class="container">
                <h1>Sign in as</h1>
                <div class="options">
                    <!-- From Uiverse.io by ElSombrero2 -->
                    <a href="/student/signin" class="card" id="card">
                        <div class="content">
                            <i class="fa-solid fa-graduation-cap"></i>
                            <p>STUDENT</p>
                        </div>
                    </a>
                    <!-- From Uiverse.io by ElSombrero2 -->
                    <a href="/student/signin" class="card" id="card">
                        <div class="content">
                            <i class="fa-solid fa-chalkboard-user"></i>
                            <p>TEACHER</p>
                        </div>
                    </a>

                </div>
            </div>
        </main>

        
    </body>

    </html>