<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;

        }

        :root {
            --color1: #333;
            --color2: #ba8254;
            --color3: #ffbf00;
            --color4: #f8f9eb;
            --color5: #375fe7;
        }

        body {
            height: 100vh;
        }

        .container {
            width: min(300px, 60vw);
            height: 15rem;
            position: relative;
            top: 50%;
            left: 50%;
            translate: -50% -50%;
            padding: 20px 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 5px 1px var(--color5);
            scale: 1.3;
        }

        form {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        h2 {
            position: relative;
            bottom: 50px;
        }

        input,
        button {
            /* width: 80%; */
            height: 30px;
            margin: 10px 0px;
            padding: 8px 20px;
            border: none;
            background-color: var(--color4);
            border-radius: 30px;
            outline: none;
            transition: all .2s;

            /* box-shadow: 0px 0px 5px 1px rgb(5, 103, 103); */

        }

        label {
            position: relative;
            left: 20px;
            color: var(--color2);
            transition: all .3s;
        }

        label:has(+ *:focus) {
            color: var(--color3);
            font-size: 14px;

        }

        button {
            background-color: var(--color5);
            font-size: 14px;
            color: white;
            cursor: pointer;
        }
    </style>
</head>



</html>